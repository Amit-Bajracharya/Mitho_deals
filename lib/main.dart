import 'package:flutter/material.dart';

void main(){
  runApp(MithoDeals());
}


class MithoDeals extends StatelessWidget {
  const MithoDeals({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: ,,
    );
  }
}